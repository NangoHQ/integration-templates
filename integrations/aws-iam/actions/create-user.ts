import type { NangoAction, User, BillCreateUser } from '../../models';

export default async function runAction(nango: NangoAction, input: BillCreateUser): Promise<User> {
    // Integration code goes here
}

integrations:
    linkedin:
        actions:
            video:
                description: Create a linkedin post with a video
                input: LinkedinVideoPost
                output: LinkedinCreatePost
                endpoint: POST /videos
                scopes:
                    - openid
                    - profile
                    - w_member_social
                    - email

models:
    LinkedinVideoPost:
        linkedinPostText: string
        videoFilePath: string
        postTtile: string

    CreateLinkedInPostWithVideoResponse:
        succcess: boolean
        postId: string

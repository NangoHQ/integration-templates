integrations:
    docusign:
        syncs:
            users:
                runs: every day
                description: |
                    Fetches a list of users from DocuSign
                output: User
                track_deletes: true
                sync_type: full
                endpoint: GET /users
                version: 2.1.0

models:
    # Users
    User:
        id: string
        firstName: string
        lastName: string
        email: string

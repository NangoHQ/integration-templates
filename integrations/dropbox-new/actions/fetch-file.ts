import type { NangoAction, Document, DocumentMetadata } from '../../models';

export default async function runAction(nango: NangoAction, input: DocumentMetadata): Promise<Document> {
    // Integration code goes here
}

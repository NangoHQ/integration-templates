export interface SyncMetadata_shopify_orders {
};

export interface Order {
  id: string;
  status: string;
  total_amount: number;
  currency: string;
  created_at: string;
  modified_at: string;
};
